##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$


windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}



# Ignore maximize requests from apps. You'll probably like this.
#windowrule = suppressevent maximize, class:.*
#windowrule = match:class .*, suppress_event maximize

# Fix some dragging issues with XWayland
#windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
#windowrule = match:class ^$, match:title ^$, match:xwayland true, match:float true, match:fullscreen false, match:pin false, no_focus on


# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = match:float,class:^(kitty)$,match:title kitty)$

# Ignore maximize requests from apps. You'll probably like this.
#windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
#windowrule = nofocus,class:^$,title:^$,xwayland:1,match:floating:1,fullscreen:0,pinned:0


windowrule = workspace 10, match:class org.telegram.desktop
# windowrule = match:float,match:class org.telegram.desktop)$
# windowrule = move 10 40,match:class org.telegram.desktop)$
# windowrule = size 1270 1390,match:class org.telegram.desktop)$

# Remmina

windowrule = workspace 9, match:class org.remmina.Remmina

# windowrule = float, class class org.pulseaudio.pavucontrol

windowrule {
  name = Volume-Control
  match:class = org.pulseaudio.pavucontrol
  float = true
  move = 1283 40
  size = 1270 679
  animation = slide right
}

# windowrule = match:float, match:class org.pulseaudio.pavucontrol


# windowrule = opacity 0.9 0.9, match:class org.pulseaudio.pavucontrol
# windowrule = move 1283 40, match:class org.pulseaudio.pavucontrol
# windowrule = size 1270 679, match:class org.pulseaudio.pavucontrol

# # windowrule = workspace 9, match:class PRO32 Connect Dashboard)$
# # windowrule = move 10 40,match:class PRO32 Connect Dashboard)$
# # windowrule = size 450 1395,match:class PRO32 Connect Dashboard)$
# # windowrule = move 1283 40,match:class PRO32 Connect Dashboard)$, match:title .*(WKS48|NTB48).*)$


# # windowrule =  match:float,match:title home.*)$
# # windowrule = size 1600 900, match:title home.*)$

# # Thunar file manager
# windowrule = match:float, match:class thunar
# windowrule = size 1600 900, match:class thunar, match:title .*Thunar.*
# windowrule = center, match:class thunar, match:title .*Thunar.*
# windowrule = opacity 0.8,match:class thunar

# windowrule = opacity 0.9 0.9, match:class codium)$
# windowrule = match:float,match:class codium, match:title Add Folder to Workspace)$
# windowrule = size 1200 700,match:class codium)$, match:title Add Folder to Workspace)$


# windowrule = opacity 0.9 0.9, match:class Cursor)$
# # windowrule = match:float, match:class Cursor)$
# # windowrule = size 1600 900, match:class Cursor)$
# # windowrule = move 1283 40, match:class Cursor)$
# # windowrule = size 1270 679, match:class Cursor)$

# # Alacritty terminal

windowrule {
  name = Terminal-Alacritty
  match:class = Alacritty
  float = true
  size = 1600 900
  center = true
  animation = popin
}


# windowrule = match:float, match:class Alacritty)$
# windowrule = size 1600 900, match:class Alacritty)$
# windowrule = center, match:class Alacritty)$

# windowrule = match:float,match:class waypaper)$
# windowrule = match:float,match:class org.gnome.eog)$ # Imageviewer-Gtki

# # windowrule = noredecorate, match:class codium)$
# windowrule = opacity 0.9, match:class WinBox)$

# # bind = $mainMod, Z, Window Management toggle match:float, togglematch:floating, #
# windowrule = match:float,match:class )$,match:title Save*)$
# windowrule = match:float,match:class )$,match:title Open*)$


# #Terminal kitty

# windowrule = match:float, match:class kitty)$
# # windowrule = opacity 0.7, match:class kitty)$
# windowrule = size 1600 900, match:class kitty)$

# windowrule = match:float, match:class swayimg)$





# Video player (MPV)
windowrule {
  name = VideoMPV
  match:class = mpv
  float = true
  center = true
  animation = popin
}

#windowrule = match:float, center,match:class mpv)$
#windowrule = opacity 0.9 override,match:class mpv)$


# # windowrule = match:float, class:(Yandex-browser)
# # windowrule = center, class:(Yandex-browser)


